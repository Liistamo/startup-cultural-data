# Core and geo dependencies for the project
# If you only run 03_csv_to_geojson.py, no external deps are strictly required,
# but these keep the environment consistent across scripts.

pandas>=2.1,<3
geopandas>=0.14,<1
shapely>=2.0,<3
fiona>=1.9,<2
pyproj>=3.5,<4
Rtree>=1.2.0
duckdb>=1.1.0,<2

# Useful (optional but recommended)
pyarrow>=15,<20
requests>=2.31,<3
python-dotenv>=1.0,<2
